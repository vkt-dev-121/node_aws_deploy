{
  "name": "aws-deploy",
  "version": "1.0.0",
  "description": "nodejs aws deployment by github actions ci/cd pipeline self hosted",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vkr-dev",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
